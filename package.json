{
  "name": "app-auth",
  "version": "4.2.0",
  "description": "Application authorization and authentication",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover _mocha",
    "mocha": "mocha -G -w",
    "postversion": "git push && git push --tags"
  },
  "author": "Tuple Labs, LLC <hello@wearetuple.co>",
  "dependencies": {
    "access-policy": "3.1.0",
    "jsonwebtoken": "^5.4.1",
    "uuid-v4": "^0.1.0"
  },
  "devDependencies": {
    "istanbul": "^0.4.0",
    "mocha": "^2.3.3"
  }
}
